#include "wcc.hpp"
#include <sstream>

using namespace std;

token* GenerateTokens(string src_s[]){ // src[] -- print("hello world");\n a=1 ...
    // generate tokens tree of string array
    
    
    return nullptr;
}

bool ParseToken(Node *n,token* toks){
    // parse tokens and generate node tree from this node to last node 
    return true;
}

// void Token::Generate(Location loc,TokenType t,void *value){

// }